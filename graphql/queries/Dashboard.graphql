query DashboardData {
  dashboard {
    summary {
      totalTorrents
      torrentsToday
      indexedLastHour
      indexedPreviousHour
      classifiedPercent
      queueProcessed
      queuePending
      queueFailed
    }
  }
}
