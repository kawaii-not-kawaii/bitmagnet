# Decision Log — spec-llm-classifier-throughput

## 2026-06-17 — Create

Distilled from the brainstorming session (First Principles + Pre-Mortem) at
`../../brainstorming/brainstorming-session-2026-06-17-174649.md`, itself grounded in a
live investigation of `root@tower`. Express distillation (input was rich; no elicitation
needed). 4 capabilities, 1 spec-authored companion (`investigation.md`).

**Key framing decision:** the originally-requested adaptive batcher is captured as an
explicit Non-goal, not a Capability — live first-principles analysis showed it would
reduce throughput on a 4-slot continuous-batching server. The spec reorients to
slot-saturation + generation-cost reduction, with an instrument-first constraint.

### Self-validate

**Pass 1 — Coherence (Spec Law 1–6, 8).** Each capability carries both intent and
success (rule 1). Intents state WHAT — measure, minimize, keep stable, decide — not HOW
(rule 2); concrete implementation surfaces are quarantined in `investigation.md`'s code
map. Constraints all bend decisions: instrument-first gates merge order, the
do-not-raise-max_context rule rules out an obvious-but-wrong lever, the concurrency-≥-slots
floor, and correctness/timeout bounds (rule 3). Five explicit Non-goals (rule 4). Success
signal is testable against `llm_classify_duration_seconds` and job-latency distributions
before/after (rule 5). CAP IDs 1–4 unique and stable (rule 6). Prose kept lean (rule 8).
Three assumptions and three open questions recorded rather than invented away.

**Pass 2 — Preservation.** All load-bearing claims from the session landed: ranked levers
→ CAP-2/3/4 + constraints; instrument-first guardrail → CAP-1 + constraint; live topology,
measured rates, capacity model, code map, and rejected-batcher rationale → `investigation.md`;
the two side-bugs (`process_torrent_batch` duplicate key, invalid-tag-name output) →
Non-goals + investigation findings so they are on record but out of scope. Wrapper-only
content (technique-selection ceremony, facilitation dialogue) intentionally dropped.

**Verdict:** Coherent and preservation-complete. Ready for downstream. The three open
questions (compute-bound vs slot-bound; latency attribution; whether `max_context` gates
anything today) are the natural first work items and are answered by CAP-1's instrumentation.
